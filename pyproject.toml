[project]
name = "datwily"
version = "0.1.0"
description = "Data preparation and cleaning library"
authors = [{name="Hermano Castro"}]
dependencies = ["pandas"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]